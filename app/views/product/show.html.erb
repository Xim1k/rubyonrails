<h1><%= @product.name %></h1>
<h2><%= @product.quantity %></h4>
<h4>"<%= @product.price %>"</h4>
<%= link_to 'Remove', @product, method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'Back', product_index_path %>
